---
- name: "Ensure Python and pip are installed"
  become: true
  ansible.builtin.apt:
    name:
      - python3
      - python3-pip
      - python3-setuptools
      - python3-venv
    state: present
    update_cache: true
